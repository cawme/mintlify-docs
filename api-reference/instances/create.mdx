---
title: "Create Instance"
description: "POST /v1/instances — Register a new WhatsApp calling instance"
openapi: "POST /v1/instances"
---

`POST /v1/instances`

Creates a new WhatsApp instance. After creation, [pair a phone number](/api-reference/instances/configure) by scanning the QR code.

## Request

### Headers

| Header | Value |
|---|---|
| `Authorization` | `Bearer <token>` |
| `Content-Type` | `application/json` |

### Body

| Field | Type | Required | Description |
|---|---|---|---|
| `name` | `string` | Yes | A label for this instance |
| `metadata` | `object` | No | Arbitrary key-value metadata |

```json
{
  "name": "sales-team-a",
  "metadata": {
    "region": "us-east"
  }
}
```

## Response `201`

Returns the created instance object.

| Field | Type | Description |
|---|---|---|
| `id` | `string` | Unique instance ID (`inst_` prefix) |
| `name` | `string` | Instance label |
| `status` | `string` | Always `CREATED` on creation |
| `metadata` | `object` | Provided metadata |
| `created_at` | `string` | ISO 8601 timestamp |
| `updated_at` | `string` | ISO 8601 timestamp |

```json
{
  "id": "inst_abc123",
  "name": "sales-team-a",
  "status": "CREATED",
  "metadata": { "region": "us-east" },
  "created_at": "2026-03-01T10:00:00Z",
  "updated_at": "2026-03-01T10:00:00Z"
}
```

## Errors

| Code | Description |
|---|---|
| `400` | `name` is missing |
| `401` | Missing or invalid token |

## Example

```bash
curl -X POST https://cawme.com/api/v1/instances \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "sales-team-a",
    "metadata": { "region": "us-east" }
  }'
```
